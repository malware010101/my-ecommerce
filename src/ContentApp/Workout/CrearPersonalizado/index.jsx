import { Container, Typography } from "@mui/material";

export default function CrearPersonalizado() {
    return (
        <Container maxWidth="lg" >
            <Typography variant="h4">Aqui van mi entrenamiento para un usuario</Typography>
        </Container>
    )
}